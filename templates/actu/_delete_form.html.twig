<form method="post" action="{{ path('actu_delete', {'id': actu.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer cette actualité ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actu.id) }}">
    <button class="btn-danger" style="width:30px;height:30px;"><i class="fas fa-trash"></i></button>
</form>
