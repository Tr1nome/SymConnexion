{% extends 'base.html.twig' %}

{% block title %}Imagetype{% endblock %}

{% block body %}
    <h1>Imagetype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imagetype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ imagetype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('imagetype_index') }}">back to list</a>

    <a href="{{ path('imagetype_edit', {'id': imagetype.id}) }}">edit</a>

    {{ include('imagetype/_delete_form.html.twig') }}
{% endblock %}
