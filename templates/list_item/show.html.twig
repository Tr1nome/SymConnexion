{% extends 'base.html.twig' %}

{% block title %}ListItem{% endblock %}

{% block body %}
    <h1>ListItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ list_item.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ list_item.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('list_item_index') }}">back to list</a>

    <a href="{{ path('list_item_edit', {'id': list_item.id}) }}">edit</a>

    {{ include('list_item/_delete_form.html.twig') }}
{% endblock %}
