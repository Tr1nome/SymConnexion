{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
    <h1>{{ formation.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <td>{% if formation.image %}<img src="{{ asset(formation.image.imgPath) }}"/>{% else %} pas d'image pour cette formation {% endif %}</td>
            </tr>
            <tr>
                <td>{{ formation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formation_index') }}">Retourner à la page formation</a>
    {% if is_granted("IS_AUTHENTICATED_FULLY") %}
    {% if is_granted("ROLE_ADMIN") %}
    <a href="{{ path('formation_edit', {'id': formation.id}) }}"><button class="btn btn-primary"><i class="fas fa-edit"></i></button></a>

    {{ include('formation/_delete_form.html.twig') }}
    {% endif %}
    {% endif %}
{% endblock %}
