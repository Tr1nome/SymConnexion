{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Edition d'une formation{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.6.4/css/bootstrap-datepicker.css">
{% endblock %}

{% block body %}
    <h1>Edition d'une Formation</h1>
        <div>
            {% if formation.image %}
            <img src="{{ asset(formation.image.imgPath) }}" style="width:200px;height:auto;" />
            {{ include('formation/_delete_img_form.html.twig') }}
            {% endif %}
        </div>    
        <div style="margin-top:30px;"></div>
        {{ include('formation/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('formation_index') }}">Retour</a>

{% endblock %}
{% block javascripts %}
{% endblock %}
