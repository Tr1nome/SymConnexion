{% extends 'base.html.twig' %}

{% block title %}Survey{% endblock %}

{% block body %}
    <h1>Survey</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ survey.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ survey.note }}</td>
            </tr>
            <tr>
                <th>Commentary</th>
                <td>{{ survey.commentary }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('survey_index') }}">back to list</a>

    <a href="{{ path('survey_edit', {'id': survey.id}) }}">edit</a>

    {{ include('survey/_delete_form.html.twig') }}
{% endblock %}
