{% extends 'base.html.twig' %}

{% block title %}MediaType{% endblock %}

{% block body %}
    <h1>MediaType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ media_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ media_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('media_type_index') }}">back to list</a>

    <a href="{{ path('media_type_edit', {'id': media_type.id}) }}">edit</a>

    {{ include('media_type/_delete_form.html.twig') }}
{% endblock %}
